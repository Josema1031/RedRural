import { db } from '../../firebase-init.js';
import { collection, onSnapshot, query, where } from 'https://www.gstatic.com/firebasejs/10.12.2/firebase-firestore.js';
const num=(v,d=0)=>{const n=Number(v);return Number.isFinite(n)?n:d};
const money=v=>`$${Math.round(num(v)).toLocaleString('es-AR')}`;
const escapeHtml=(str='')=>String(str).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');
function monthOk(s,now=new Date()){const ms=num(s.finalizadoEnMs)||num(s.finMs)||num(s.inicioMs)||num(s.enCaminoMs)||(num(s.createdAt?.seconds)*1000)||(num(s.timestamp?.seconds)*1000);if(!ms)return true;const d=new Date(ms);return d.getMonth()===now.getMonth()&&d.getFullYear()===now.getFullYear()}
const pct=(v,t)=>!t?0:Math.max(0,Math.min(100,(v/t)*100));
const calcImporte=s=>num(s.importeTotal,num(s.totalCobrado));
const calcCosto=s=>Number.isFinite(Number(s.costoTotalOperativo))?num(s.costoTotalOperativo):Math.round(num(s.distanciaKm,num(s.distanciaM)/1000)*950+num(s.duracionMin)*140+num(s.tarifaBase)*.2);
const serviceLabel=s=>String(s.nombreCampo||s.campo||'Sin campo').trim()||'Sin campo';
function ensureContainer(id,place,parentSel){let n=document.getElementById(id);if(n)return n;n=document.createElement('section');n.id=id;n.className='v27-shell';(document.querySelector(parentSel)||document.body).insertAdjacentElement(place,n);return n}
function topMapRows(map){const max=Math.max(...Array.from(map.values()),1);return Array.from(map.entries()).sort((a,b)=>b[1]-a[1]).slice(0,5).map(([name,value])=>({name,value,width:pct(value,max)}))}
function bars(rows){if(!rows.length)return '<div class="v27-muted">Todavía no hay datos del mes para mostrar.</div>';return `<div class="v27-bars">${rows.map(r=>`<div class="v27-bar-row"><div class="v27-bar-top"><strong>${escapeHtml(r.name)}</strong><span>${escapeHtml(r.label)}</span></div><div class="v27-bar-track"><div class="v27-bar-fill" style="width:${num(r.width)}%"></div></div></div>`).join('')}</div>`}
function list(rows,empty='Sin información todavía.'){if(!rows.length)return `<div class="v27-muted">${escapeHtml(empty)}</div>`;return `<div class="v27-list">${rows.map(r=>`<div class="v27-list-item"><div><p class="v27-list-title">${escapeHtml(r.name)}</p><p class="v27-list-sub">${escapeHtml(r.label)}</p></div><div class="v27-list-value">${escapeHtml(r.valueText||'')}</div></div>`).join('')}</div>`}
function subscribeServices(productorId,onData){const q=query(collection(db,'solicitudesPatrulla'),where('productorId','==',productorId));return onSnapshot(q,s=>onData(s.docs.map(d=>({id:d.id,...d.data()}))),()=>onData([]))}
function producerStats(items){const m=items.filter(monthOk);let total=m.length,finalizadas=0,activas=0,urgentes=0,fact=0,costo=0,km=0;const campos=new Map(),motivos=new Map();for(const s of m){const e=String(s.estado||'').toLowerCase();if(e==='finalizada')finalizadas++;else activas++;if(String(s.prioridad||'').toLowerCase()==='alta')urgentes++;fact+=calcImporte(s);costo+=calcCosto(s);km+=num(s.distanciaKm,num(s.distanciaM)/1000);const c=serviceLabel(s);campos.set(c,(campos.get(c)||0)+1);const mot=String(s.motivo||s.tipo||s.tipoServicio||'Patrulla general');motivos.set(mot,(motivos.get(mot)||0)+1)}return{total,finalizadas,activas,urgentes,fact,costo,margen:fact-costo,km,efectividad:total?Math.round(finalizadas/total*100):0,topCampos:topMapRows(campos).map(r=>({...r,label:`${r.value} servicios`,valueText:`${r.value} servicios`})),topMotivos:topMapRows(motivos).map(r=>({...r,label:`${r.value} casos`,valueText:`${r.value} casos`})),funnel:[['Solicitudes',total],['Finalizadas',finalizadas],['Activas',activas],['Urgentes',urgentes]]}}
function employeeStats(items,empleadoDni){const m=items.filter(monthOk).filter(s=>String(s.patrulleroDni||s.asignadoPatrulleroDni||'')===String(empleadoDni||''));let total=m.length,finalizadas=0,activas=0,urgentes=0,km=0,minutos=0,pago=0;const campos=new Map();for(const s of m){const e=String(s.estado||'').toLowerCase();if(e==='finalizada')finalizadas++;else activas++;if(String(s.prioridad||'').toLowerCase()==='alta')urgentes++;km+=num(s.distanciaKm,num(s.distanciaM)/1000);minutos+=num(s.duracionMin,(num(s.finMs)-num(s.inicioMs))/60000);pago+=num(s.pagoPatrullero,calcImporte(s)*.35);const c=serviceLabel(s);campos.set(c,(campos.get(c)||0)+1)}return{total,finalizadas,activas,urgentes,km,minutos,pago,cumplimiento:total?Math.round(finalizadas/total*100):0,topCampos:topMapRows(campos).map(r=>({...r,label:`${r.value} patrullas`,valueText:`${r.value} patrullas`})),funnel:[['Tomadas',total],['Finalizadas',finalizadas],['Activas',activas],['Urgentes',urgentes]]}}
function renderProducer(root,stats){const rows=stats.funnel.map(([name,value])=>({name,label:String(value),width:pct(value,Math.max(...stats.funnel.map(v=>v[1]),1))}));root.innerHTML=`<div class="v27-hero"><div class="v27-hero-head"><div><div class="v27-eyebrow">V27 · visual premium</div><h2 class="v27-title">Centro visual del productor</h2><p class="v27-subtitle">RED RURAL entra en su capa de visualización premium: lectura rápida, KPIs fuertes y una imagen más vendible para mostrar el sistema.</p></div><div class="v27-badge-stack"><div class="v27-badge"><div class="v27-badge-label">Estado</div><div class="v27-badge-value">Operativo</div></div><div class="v27-badge"><div class="v27-badge-label">Efectividad</div><div class="v27-badge-value">${stats.efectividad}%</div></div></div></div><div class="v27-kpis"><div class="v27-kpi"><div class="v27-kpi-label">Servicios del mes</div><div class="v27-kpi-value">${stats.total}</div><div class="v27-kpi-foot">${stats.finalizadas} cerradas · ${stats.activas} activas</div></div><div class="v27-kpi"><div class="v27-kpi-label">Facturación estimada</div><div class="v27-kpi-value">${money(stats.fact)}</div><div class="v27-kpi-foot">Margen: ${money(stats.margen)}</div></div><div class="v27-kpi"><div class="v27-kpi-label">Kilómetros</div><div class="v27-kpi-value">${Math.round(stats.km).toLocaleString('es-AR')} km</div><div class="v27-kpi-foot">Seguimiento operativo consolidado</div></div><div class="v27-kpi"><div class="v27-kpi-label">Urgencias</div><div class="v27-kpi-value">${stats.urgentes}</div><div class="v27-kpi-foot">Foco en sectores críticos</div></div></div></div><div class="v27-grid"><div class="v27-card"><div class="v27-card-header"><h3 class="v27-card-title">Embudo operativo del mes</h3><span class="v27-chip ${stats.efectividad<45?'danger':stats.efectividad<70?'warning':''}">${stats.efectividad}% de cierre</span></div>${bars(rows)}</div><div class="v27-card"><div class="v27-card-header"><h3 class="v27-card-title">Lectura rápida</h3></div><div class="v27-mini-grid"><div class="v27-mini"><div class="v27-mini-label">Costo operativo</div><div class="v27-mini-value">${money(stats.costo)}</div></div><div class="v27-mini"><div class="v27-mini-label">Margen estimado</div><div class="v27-mini-value">${money(stats.margen)}</div></div><div class="v27-mini"><div class="v27-mini-label">Activas</div><div class="v27-mini-value">${stats.activas}</div></div><div class="v27-mini"><div class="v27-mini-label">Urgentes</div><div class="v27-mini-value">${stats.urgentes}</div></div></div></div><div class="v27-card"><div class="v27-card-header"><h3 class="v27-card-title">Campos con mayor demanda</h3><span class="v27-chip">Hotspots</span></div>${list(stats.topCampos,'Aún no hay campos con actividad este mes.')}</div><div class="v27-card"><div class="v27-card-header"><h3 class="v27-card-title">Motivos más frecuentes</h3><span class="v27-chip warning">Prioridades</span></div>${list(stats.topMotivos,'Todavía no hay motivos registrados.')}</div></div>`}
function renderEmployee(root,stats){const rows=stats.funnel.map(([name,value])=>({name,label:String(value),width:pct(value,Math.max(...stats.funnel.map(v=>v[1]),1))}));const promedio=stats.finalizadas?Math.round(stats.minutos/Math.max(stats.finalizadas,1)):0;root.innerHTML=`<div class="v27-hero"><div class="v27-hero-head"><div><div class="v27-eyebrow">V27 · visual premium</div><h2 class="v27-title">Panel visual del patrullero</h2><p class="v27-subtitle">Vista ejecutiva para trabajar más rápido: cumplimiento, patrullas del mes y zonas donde más actividad tuviste.</p></div><div class="v27-badge-stack"><div class="v27-badge"><div class="v27-badge-label">Cumplimiento</div><div class="v27-badge-value">${stats.cumplimiento}%</div></div><div class="v27-badge"><div class="v27-badge-label">Pago estimado</div><div class="v27-badge-value">${money(stats.pago)}</div></div></div></div><div class="v27-kpis"><div class="v27-kpi"><div class="v27-kpi-label">Patrullas tomadas</div><div class="v27-kpi-value">${stats.total}</div><div class="v27-kpi-foot">${stats.finalizadas} cerradas · ${stats.activas} activas</div></div><div class="v27-kpi"><div class="v27-kpi-label">Kilómetros</div><div class="v27-kpi-value">${Math.round(stats.km).toLocaleString('es-AR')} km</div><div class="v27-kpi-foot">Recorrido acumulado del mes</div></div><div class="v27-kpi"><div class="v27-kpi-label">Minutos operativos</div><div class="v27-kpi-value">${Math.round(stats.minutos).toLocaleString('es-AR')}</div><div class="v27-kpi-foot">Promedio por cierre: ${promedio} min</div></div><div class="v27-kpi"><div class="v27-kpi-label">Urgencias</div><div class="v27-kpi-value">${stats.urgentes}</div><div class="v27-kpi-foot">Servicios de alta prioridad</div></div></div></div><div class="v27-grid"><div class="v27-card"><div class="v27-card-header"><h3 class="v27-card-title">Embudo personal del mes</h3><span class="v27-chip ${stats.cumplimiento<50?'danger':stats.cumplimiento<75?'warning':''}">${stats.cumplimiento}% cumplimiento</span></div>${bars(rows)}</div><div class="v27-card"><div class="v27-card-header"><h3 class="v27-card-title">Lectura rápida</h3></div><div class="v27-mini-grid"><div class="v27-mini"><div class="v27-mini-label">Finalizadas</div><div class="v27-mini-value">${stats.finalizadas}</div></div><div class="v27-mini"><div class="v27-mini-label">Activas</div><div class="v27-mini-value">${stats.activas}</div></div><div class="v27-mini"><div class="v27-mini-label">Urgentes</div><div class="v27-mini-value">${stats.urgentes}</div></div><div class="v27-mini"><div class="v27-mini-label">Pago estimado</div><div class="v27-mini-value">${money(stats.pago)}</div></div></div></div><div class="v27-card"><div class="v27-card-header"><h3 class="v27-card-title">Campos donde más trabajaste</h3><span class="v27-chip">Ritmo</span></div>${list(stats.topCampos,'Todavía no hay patrullas suficientes este mes.')}</div><div class="v27-card"><div class="v27-card-header"><h3 class="v27-card-title">Mapa de intensidad</h3><span class="v27-chip warning">Zonas</span></div>${stats.topCampos.length?`<div class="v27-heat">${stats.topCampos.map(r=>`<div class="v27-heat-item"><strong>${escapeHtml(r.name)}</strong><span>${escapeHtml(r.label)}</span><div class="v27-heat-meter"><div class="v27-heat-fill" style="width:${num(r.width)}%"></div></div></div>`).join('')}</div>`:'<div class="v27-muted">Sin datos suficientes para el mapa de intensidad.</div>'}</div></div>`}
export function initV27ProducerVisual({productorId}){
  if(!productorId) return;
  document.body.classList.add('v27-panel-premium');
  
  // En lugar de crear un div nuevo, usamos el tuyo de panel.html
  const root = document.getElementById('contenedorV27');
  
  if(root) {
    subscribeServices(productorId, items => renderProducer(root, producerStats(items)));
  }
}export function initV27EmployeeVisual({productorId,empleadoDni}){if(!productorId||!empleadoDni)return;document.body.classList.add('v27-panel-premium');const root=ensureContainer('v27EmployeeVisual','afterbegin','.wrap');subscribeServices(productorId,items=>renderEmployee(root,employeeStats(items,empleadoDni)))}
