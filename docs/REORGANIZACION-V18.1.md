# V18.1

- Corregido error `db is not defined` en `js/modules/empleado/index.js`.
- Se importa `auth` y `db` desde `firebase-init.js`.
- Se obtiene `empleadoDni` desde `localStorage` antes de montar servicios del empleado.
- Se mantiene el formato ZIP completo con carpeta raíz `RED RURAL`.
